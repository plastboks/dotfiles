#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

# gpg agent
#rm ~/.gpg-agent-info
#gpg-agent --daemon --enable-ssh-support --use-standard-socket --write-env-file

# source bashrc 
source $HOME/.bashrc
